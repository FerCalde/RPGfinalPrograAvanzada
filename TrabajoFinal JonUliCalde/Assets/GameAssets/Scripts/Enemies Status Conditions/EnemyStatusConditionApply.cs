using System.Collections;
using System.Collections.Generic;
using UnityEngine;


namespace enemyStatusConditions
{
    /*
    public abstract class EnemyStatusConditionApply : EnemyStatusCondition, IStun, IPoison
    {

        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }

        public void Stun(GameObject gOToStun)
        {

            //Check enemy is not stunned, it does not have to be false, but the check. Access enemyvar to check if it has any state or not to apply the effect.
            bool isStunned = false; //Something like gOToStun.instance.GetComponent<state> != null, we set the correct effect to it.


            if (!isStunned)
            {
                //Change State.
            }
        }

        public void Poison (GameObject gOToPpoison)
        {
            
        }

        
    }
    */
}
